/** 약관·방침 문서 경로. 로그인 화면·`/me` 링크와 라우트가 어긋나지 않게 한곳에 모은다. */
export const TERMS_PATH = '/terms';

export const PRIVACY_PATH = '/privacy';

/** 두 문서가 함께 쓰는 시행일. 조항이 바뀌면 이 값도 함께 올린다. */
export const LEGAL_EFFECTIVE_DATE = '2026년 8월 31일';

/** 문의·열람·삭제 요청을 받는 운영자 연락처. */
export const LEGAL_CONTACT_EMAIL = 'doublejbjy@gmail.com';
