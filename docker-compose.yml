services:
  postgres:
    image: postgres:16-alpine
    container_name: side-postgres
    environment:
      POSTGRES_USER: side
      POSTGRES_PASSWORD: side
      POSTGRES_DB: side
    ports:
      - "5432:5432"
    volumes:
      - side-pgdata:/var/lib/postgresql/data

volumes:
  side-pgdata:
